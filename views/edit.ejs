<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
      <%- include('partials/header.ejs') %>
       <div class="container-table">
         <h1>Edit Book</h1>
            <form  class="form-table box" action="/edit/<%= book.id %>" method="POST">
                <input type="text" name="title" placeholder="Book Title" value="<%= book.title %>" required>
                <input type="text" name="author" placeholder="Author" value="<%= book.author %>">
                <input type="number" name="rating" placeholder="Rating" value="<%= book.rating %>" required>
                <textarea name="notes" placeholder="Your notes" required><%= book.notes%></textarea>
                <input type="date" name="date_read"  value="<%= new Date().toISOString().split('T')[0] %>" readonly>
                <button type="submit">Update Book</button>
            </form>
       </div>
       <%- include('partials/footer.ejs') %>
    
</body>
</html>